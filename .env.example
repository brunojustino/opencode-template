# API keys for opencode MCP servers.
# Copy this file to .env and fill in your keys. .env is gitignored - never commit real keys.

# Context7 (library docs MCP): works keyless when empty (rate-limited).
# Free key: https://context7.com
CONTEXT7_API_KEY=

# Serena needs no key - it runs locally via uv.
